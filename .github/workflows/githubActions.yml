name: UI build

on:
  # pull_request:
  #   branches: [ 'master' ]
  #   paths: 'React_UI/**'
  #   types:
  #     - opened
  push: 
    branches: ['Test']

jobs:
  build:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: npm install
      run: npm install
      with:
        workingDirectory: React_UI

    # - name: npm run build
    #   run: npm run build
